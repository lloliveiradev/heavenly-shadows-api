import { Local } from "./local";

export interface Capital {
    descricao: string;
    img: string;
    locais: Local[];
    nome: string;
    resumo: string;
    subtitulo: string;
    titulo: string;
}